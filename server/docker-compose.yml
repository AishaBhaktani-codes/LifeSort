version: '3.8'

services:
  db:
    image: pgvector/pgvector:pg16
    container_name: lifesort-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: lifesort
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d lifesort"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
