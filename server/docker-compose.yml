version: '3.8'

services:
  # The Node.js Backend Server
  server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    env_file:
      - .env
    restart: unless-stopped
